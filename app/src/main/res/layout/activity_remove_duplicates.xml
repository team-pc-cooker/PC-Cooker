<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    android:background="#FFFFFF">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Remove Duplicate Components"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="This will scan the Firebase database and remove all duplicate components with the same name. Only the first occurrence of each component will be kept."
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center"
        android:layout_marginBottom="32dp"
        android:padding="16dp" />

    <Button
        android:id="@+id/removeDuplicatesButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Remove Duplicates"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:background="#FF5722"
        android:padding="16dp"
        android:layout_marginBottom="32dp" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ready to scan for duplicates"
        android:textSize="14sp"
        android:textColor="#2196F3"
        android:gravity="center"
        android:padding="16dp" />

</LinearLayout> 